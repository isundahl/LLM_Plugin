Do not store model weights in this Content directory.

Configure an external .gguf path in Project Settings > Plugins > Local Multimodal LLM.
For packaged builds, stage approved weights explicitly as NonUFS files.
